# Enable rewrite engine
RewriteEngine On

# Redirect all requests to index.php (API router)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
